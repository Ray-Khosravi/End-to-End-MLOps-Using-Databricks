apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: churn
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: http      # → 8080 on the pod (unprivileged nginx)
      protocol: TCP
